# URL of backend/token_server.py. Defaults to http://localhost:8000/token
# when unset (see src/hooks/use-flowvoice.ts). Set this to your deployed
# token server's HTTPS URL before deploying the frontend anywhere other
# than local dev.
VITE_TOKEN_SERVER_URL=http://localhost:8000/token
